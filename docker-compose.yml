version: '3.8'
services:
  web:
    image: yourname/nginx:latest
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    image: yourname/backend:latest
    environment:
      - VARS=HERE
